# anyduct-web — environment variables.
#
# NEXT_PUBLIC_ prefix is required for the browser bundle to see the value.
# Leave unset to default to http://localhost:8000 (matches `uvicorn anyduct_server.main:app`).

NEXT_PUBLIC_ANYDUCT_API_URL=http://localhost:8000

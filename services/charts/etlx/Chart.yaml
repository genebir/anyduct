---
apiVersion: v2
name: etlx
description: >-
  etlx services — FastAPI control plane, PG-queue workers (batch worker /
  cron scheduler / zombie reaper / stream worker), Next.js web UI, and an
  optional embedded metadata Postgres. Mirrors
  services/docker-compose.prod.yml one-to-one (ADR-0096).
type: application
version: 0.1.0
appVersion: "0.1.0"

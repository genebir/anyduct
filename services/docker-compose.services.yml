---
# SUPERSEDED (2026-06-12). This file was the Step 7.1 placeholder sketch;
# the real, exercised full-stack compose is ``docker-compose.prod.yml``
# in this directory (db → migrate → server/worker/scheduler/reaper/
# stream-worker → web, healthchecks, JWT env wiring). See
# ``docs/deployment.md`` ("One-command local production stack") for the
# run instructions, and ``../start.sh`` for the non-container dev loop.
#
# Kept as a pointer so old links/docs don't 404; do not add services here.
services: {}

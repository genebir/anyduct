"""etlx-server — FastAPI backend for the etl-plugins service layer.

Public API surface arrives in Step 7.2 onward. For now this package only
provides ``etlx_server.main:app`` with health/version endpoints.
"""

__version__ = "0.0.0"
